<h1><%= @user.email %></h1>
  
  <form action="/insert_clink_record" method="post">
        <input type="hidden" name="sender_id" value="<%= current_user.id %>">
        <input type="hidden" name="recipient_id" value="<%= @user.id %>">
          <button class="btn btn-primary">
            <i class="fas fa-glass-cheers"></i> Clink with <%= @user.email %>
          </button>
  </form>

<hr>

<table cellpadding="10">
  <tr>
    <td rowspan="5"><img src="<%= @user.profile_pic %>"></td>
    <td>Name:</td>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <td>Email:</td>
    <td><%= @user.email %></td>
  </tr>
  
  <tr>
    <td>Bio:</td>
    <td><%= @user.bio %></td>
  </tr>
  
  <tr>
    <td>Taste Profile:</td>
    <td><%= @user.taste_profile %></td>
  </tr>

  <tr>
      <td></td>
      <td>
         <a href="/users/edit">
            <button class="btn btn-primary">
                Edit Profile
            </button>
        </a>
      </td>
  </tr>
  
</table>

